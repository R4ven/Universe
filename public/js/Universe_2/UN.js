var UN = UN || { REVISION: '1' };

UN.devicePixelRatio = window.devicePixelRatio || 1;

UN.TOUCHMODES = {
	NONE : 0,
	SINGLE : 1,
	DOUBLE : 2,
}
/*
UN.IMAGEPATH = IMAGEPATH;
UN.BASE_URL = BASE_URL;
UN.IMAGEPATH = IMAGEPATH;
UN.TEXTURES = TEXTUREPATH;
UN.SHADERPATH = SHADERPATH;
UN.SHADERLIST = null;

var TOUCHMODES = {
	NONE : 0,
	SINGLE : 1,
	DOUBLE : 2,
}
var mouseX = 0, mouseY = 0, pmouseX = 0, pmouseY = 0, pressX = 0, pressY = 0, rotateVX = 0, rotateVY = 0, rotateX = 0.4, rotateY = 0.9;
var touchMode = TOUCHMODES.NONE;
var dragging = false;
var masterContainer = null;
var initialAutoRotate = true;*/
